
var app = {
    
    // Initialise the audiorecorder. This gets called repeatedly and will report an error, but this doesn't matter.
    
    initialize: function() {

        audioRecorder.initialiseAudioRecorder();
        
    }

};
